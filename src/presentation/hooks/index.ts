/**
 * @file index.ts
 * @description Barrel export for presentation-layer hooks.
 * Always import hooks from here.
 *
 * Usage:
 *   import { useAppDispatch, useAppSelector } from '@/presentation/hooks';
 */

export { useAppDispatch, useAppSelector } from '@/store/hooks';
